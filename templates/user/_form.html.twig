    {# Form pour modifier le profil #}
    <div class="text-center">
        <h3 id ="modif-profil" class="font-italic">Modifier mon profil</h3>
        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="btn-success border rounded">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>