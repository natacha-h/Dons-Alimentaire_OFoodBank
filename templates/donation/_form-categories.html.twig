{# <form action class="form-inline">

        <label for="exampleFormControlSelect1">Filtrer par catégorie</label>
        <select class="form-control" id="exampleFormControlSelect1">
            {% for category in categories %}
                <option id="choice"  data-id="{{category.id}}">{{ category.name }}</option>    
            {% endfor %}    
        </select>
        <button id="filter" class="btn btn-success"> Filtrer</button>
 
</form> #}

        {{ form_start(form, {'method': 'GET'}) }}
        <div class="form-inline">        
        {{ form_widget(form, {'attr': {'class': 'mr-3'}}) }}
        {# {{form_row(form)}} #}
           <button class="btn btn-success mr-3">{{ button_label|default('Filtrer') }}</button>
        </div>
        {{ form_end(form) }}
